# Baton Dashboard

This application provides the hosted Next.js dashboard. Its browser calls use
the public HTTP contract with HTTP-only API cookies; it must not import API
handlers, worker code, or database packages.
