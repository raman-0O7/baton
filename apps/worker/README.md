# Baton Worker

This application will run durable indexing, projection, summarization, memory,
export, and deletion jobs.

Conversation content is untrusted input and must not appear in routine logs,
traces, or job metadata.
